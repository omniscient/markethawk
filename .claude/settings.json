{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); exit(0 if 'backend' in f and f.endswith('.py') else 1)\" && docker compose -f \"C:/git/trading/MarketHawk/docker-compose.yml\" restart celery-worker 2>/dev/null || true",
            "statusMessage": "Restarting celery-worker...",
            "async": true
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  }
}
