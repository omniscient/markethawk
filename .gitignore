# OS based
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.env
.pytest_cache/
.ruff_cache/

# Node.js
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.npm
.yarn

# Logs
*.log
logs/

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Claude Code (machine-specific)
.claude/settings.local.json

# Docker
.docker/

# Project Specific
frontend/build.log
frontend/stats.html
backend/celerybeat-schedule

# Git worktrees
.worktrees/

# Archon runtime artifacts
.archon/state/
.archon/.env

# Dark factory runtime
dark-factory/tmp/

# Local artifacts (log dumps, scratch DBs)
*.db
*-logs.txt

# Repowise pilot (generated index and editor files — do not commit)
.repowise/*
!.repowise/config.yaml
.mcp.json
.claude/CLAUDE.md

# Codeindex generated artifacts — regenerate on demand (scripts/codeindex.sh or the
# factory update-codeindex pass; ~6s). Committing them caused a merge conflict on every
# PR (see #343). docs/codeindex-hotspots.md (small, human-readable) stays committed.
codeindex.json
symbolindex.json
